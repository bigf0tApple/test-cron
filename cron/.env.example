# ====================================================================
# WEEV2 RAILWAY SERVICES - ENVIRONMENT CONFIGURATION
# ====================================================================
# Copy this file to .env and fill in the values

# RPC Configuration
RPC_URL=https://mainnet.base.org

# Railway Wallet Private Key (same for RAILWAY1 and RAILWAY2)
# This wallet needs to be funded with ETH for gas
RAILWAY_PRIVATE_KEY=your_private_key_here

# ====================================================================
# RAILWAY1: EPOCH AUTOMATION (cron/index.js)
# ====================================================================
# Calls batchStartEpoch() and batchEndCycle() on schedule
REWARDS_CONTRACT=0x_your_rewards_contract_address

# ====================================================================
# RAILWAY2: ARB MONITORING (cron/arb-checker.js)  
# ====================================================================
# Polls for arb opportunities every 10 seconds
ARB_CONTRACT=0x_your_arb_contract_address
WWMM_CONTRACT=0x_your_wwmm_contract_address

# ====================================================================
# SETUP REQUIRED:
# ====================================================================
# 1. Fund the Railway wallet with ~0.1 ETH for gas
# 2. Set the wallet as keeper: wwmmContract.setKeeper(WALLET_ADDRESS, true)
# 3. Add wallet as allowed executor: rewardsContract.addAllowedExecutor(WALLET_ADDRESS)
# 4. Deploy RAILWAY1 on 5-minute cron schedule
# 5. Deploy RAILWAY2 as always-on worker
